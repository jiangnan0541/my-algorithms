# 复刻方法论（读 → 写 → 证 → 贡献）

贯穿所有复刻项目的四步法。每做一个项目，都按这个节奏走。

## 1. 读（Read）

- `git clone` 官方仓库，先跑通官方最小例子，确认环境没问题。
- 精读**核心模块**源码（不要通读全部），重点理解：
  - 模块职责边界
  - 关键数据结构
  - 主流程控制流
- 画一张**模块依赖图**（工具任选：draw.io / Excalidraw / mermaid），
  放进 `docs/architecture.md`。目标是「能给别人讲清楚它怎么串起来」。

## 2. 写（Write）

- 从零重写一个「迷你版」：**不抄实现，只对齐功能**。
- 允许用更少的特性、更直白的代码，但核心能力要对齐官方。
- 本仓库结构：

  ```
  my-algorithms/
    src/myalgo/      # 核心实现（按分类分子包）
    tests/           # pytest 单元测试
    docs/
      architecture.md   # 官方源码模块依赖图
  ```

## 3. 证（Prove）

- 同时跑通**官方小例子** + **你的 mini 版**，写对比笔记：
  - 功能是否对齐？
  - 输出/指标是否一致（误差来自哪里）？
  - 性能/资源占用差异？
- 把结论填进 README 的「与官方对比」表。

## 4. 贡献（Contribute）

- 往上游提 **1 个 PR**（修文档 / 补示例 / 改 typo 即可）——这是简历最强背书。
- 流程：`fork` → 改 → `commit` → `PR`。PR 描述写清「为什么改、改了什么」。

## 黄金提醒

1. 不要只 clone 跑通：mini 版重写 + 上游 PR 才是真经验。
2. 每个项目一个仓库，README 写清「我做了什么 / 学到什么 / 与官方对比」。
3. 医学数据只用公开数据集，不碰隐私数据。
4. GPU 只在阶段 5 租，其余全 CPU，整体成本控制在百元内。
